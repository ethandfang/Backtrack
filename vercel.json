{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "frontend/api/submit.js": { "maxDuration": 30 },
    "frontend/api/poll.js":   { "maxDuration": 30 }
  }
}
